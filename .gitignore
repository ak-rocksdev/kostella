node_modules/
.next/
out/
build/
.DS_Store
*.pem
npm-debug.log*
.env*
!.env.example
next-env.d.ts
.vercel
*.tsbuildinfo

# impeccable live-mode runtime state
.impeccable/live/state/

# Local agent-harness hooks (machine-specific absolute paths)
.cursor/
.codex/

# Browser-automation artifacts from agent sessions. Three of these were swept
# into the repo by `git add -A` and purged from history before the first push.
.playwright-mcp/

# Detail akses server — repo ini publik
server-info.md
